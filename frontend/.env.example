# Backend API base URL (no trailing slash)
# Local: VITE_BACKEND_URL=http://localhost:5050
# Production (Vercel): set in Project Settings > Environment Variables
VITE_BACKEND_URL=https://flight-booking-node-backend.onrender.com
